<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingStart="20dp"
    android:paddingTop="24dp"
    android:paddingEnd="20dp"
    android:paddingBottom="20dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/screenshot_studio_kicker"
                android:textColor="?attr/autoCompleteUrlColor"
                android:textSize="13sp" />
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/screenshot_studio_title"
                android:textColor="?attr/autoCompleteTitleColor"
                android:textSize="32sp"
                android:textStyle="bold" />
        </LinearLayout>
        <ImageButton
            android:id="@+id/close_button"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/oasisbrowser_blend_button_background"
            android:contentDescription="@string/action_cancel"
            android:src="@drawable/ic_action_delete"
            app:tint="?attr/iconColor" />
        <ImageButton
            android:id="@+id/clear_selection_button"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginEnd="8dp"
            android:background="@drawable/oasisbrowser_blend_button_background"
            android:contentDescription="@string/screenshot_studio_clear_selection"
            android:src="@drawable/ic_action_refresh"
            app:tint="?attr/iconColor" />
    </LinearLayout>

    <com.alzimerahmed.oasisbrowser.screenshot.ScreenshotCanvasView
        android:id="@+id/screenshot_canvas"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="16dp"
        android:contentDescription="@string/screenshot_studio_canvas_description" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">
        <com.google.android.material.button.MaterialButton
            android:id="@+id/save_button"
            style="@style/Widget.Material3.Button.TonalButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:minHeight="56dp"
            android:text="@string/screenshot_studio_save"
            android:textColor="?attr/colorOnSurface"
            app:backgroundTint="?attr/colorSecondaryContainer"
            app:cornerRadius="20dp" />
        <com.google.android.material.button.MaterialButton
            android:id="@+id/search_button"
            style="@style/Widget.Material3.Button.TonalButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:layout_weight="1"
            android:minHeight="56dp"
            android:text="@string/screenshot_studio_search"
            android:textColor="?attr/colorOnSurface"
            app:backgroundTint="?attr/colorSecondaryContainer"
            app:cornerRadius="20dp" />
    </LinearLayout>
</LinearLayout>
