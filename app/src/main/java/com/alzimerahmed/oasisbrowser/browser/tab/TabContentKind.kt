package com.alzimerahmed.oasisbrowser.browser.tab

/** Identifies whether a tab is showing app-owned UI or content rendered by a browser engine. */
enum class TabContentKind {
    NATIVE_HOMEPAGE,
    ENGINE,
}
